---
import { getEntry } from 'astro:content';
import DomainLayout from '../../layouts/DomainLayout.astro';

const entry = await getEntry('domain', 'medical/fr');
if (!entry) throw new Error('Missing domain content: medical/fr');
---
<DomainLayout locale="fr" data={entry.data} />
