---
import { getEntry } from 'astro:content';
import DomainLayout from '../../layouts/DomainLayout.astro';

const entry = await getEntry('domain', 'medical/en');
if (!entry) throw new Error('Missing domain content: medical/en');
---
<DomainLayout locale="en" data={entry.data} />
