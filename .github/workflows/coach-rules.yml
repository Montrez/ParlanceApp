name: Coach rules

on:
  push:
    paths:
      - 'shared/coach-rules/**'
      - 'training/coach_rules.py'
      - 'training/coach_tips.py'
      - 'training/parlance_slm_validate.py'
      - 'training/validate_coach_rules.py'
      - 'training/run_coach_rules_regression.py'
      - 'training/run_coach_regression.py'
      - 'training/golden/**'
      - 'Parlance/web/coach-rules-engine.js'
  pull_request:
    paths:
      - 'shared/coach-rules/**'
      - 'training/coach_rules.py'
      - 'training/coach_tips.py'
      - 'training/parlance_slm_validate.py'
      - 'training/validate_coach_rules.py'
      - 'training/run_coach_rules_regression.py'
      - 'training/run_coach_regression.py'
      - 'training/golden/**'
      - 'Parlance/web/coach-rules-engine.js'

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - name: Validate rule schema and sources
        run: python3 training/validate_coach_rules.py
      - name: Run deterministic rules regression (es)
        run: python3 training/run_coach_rules_regression.py --lang es
      - name: Run deterministic rules regression (fr)
        run: python3 training/run_coach_rules_regression.py --lang fr
      - name: Run golden regression heuristic (es)
        run: python3 training/run_coach_regression.py --lang es --heuristic
      - name: Run golden regression heuristic (fr)
        run: python3 training/run_coach_regression.py --lang fr --heuristic
