{
  "name": "parlance-functions",
  "description": "Parlance Firebase Cloud Functions (auth proxy for AI analysis)",
  "private": true,
  "main": "index.js",
  "engines": {
    "node": "20"
  },
  "scripts": {
    "lint": "node -c index.js && node -c lib/prompts.js && node -c lib/parse.js && node -c lib/analyze.js"
  },
  "dependencies": {
    "firebase-admin": "^13.4.0",
    "firebase-functions": "^6.3.2"
  }
}
