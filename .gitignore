# Secrets — API keys and credentials (never commit)
Parlance/Secrets.plist
Secrets.plist
Parlance/GoogleService-Info.plist
GoogleService-Info.plist
docs/firebase-config.js
Parlance/web/firebase-config.js
firebase/functions/.env
firebase/functions/.env.*

# Xcode build artifacts
build/
*.xcuserstate
xcuserdata/
DerivedData/
*.moved-aside
*.pbxuser
!default.pbxuser
*.mode1v3
!default.mode1v3
*.mode2v3
!default.mode2v3
*.perspectivev3
!default.perspectivev3

# Training data and model artifacts
training/data/
training/adapters/
training/models/
Parlance/Models/parlance-es-mlx/
Parlance/Models/parlance-fr-mlx/
training/models/parlance-fr-mlx/
training/checkpoints/
training/*.log
training/*.zip

# Xcode DerivedData / SourcePackages archives (xcodebuild -resultBundlePath, CI artifacts)
output/

# Firebase CLI debug logs
firebase/*.log
firebase/firepit-log.txt
firebase/functions/firebase-debug.log

# Node / Capacitor
node_modules/

# Android build artifacts
android/.gradle/
android/app/build/
android/build/
android/local.properties
android/.idea/
*.apk
*.aab
*.keystore
*.jks

# macOS
.DS_Store

# Cursor agent session state (plans, local rules drafts) — local-only
.cursor/
